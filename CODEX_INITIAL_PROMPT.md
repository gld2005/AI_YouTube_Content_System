# Initial Prompt for Codex

Read `AGENTS.md`, every document in `docs/`, and `reference/BASELINE_NOTES.md`. Inspect the reference workbook without overwriting it.

Your first deliverable is a proposed implementation plan for approval. Do not implement or modify files yet.

The plan must include:

- your proposed architecture for the macro-enabled workbook, VBA modules, local Windows assistant, Python source mode, and optional cloud-compatible interface;
- workbook migration and data-preservation strategy;
- a requirement traceability check showing how every requirement group will be delivered and tested;
- technical risks, Excel limitations, security risks, and proposed mitigations;
- the files and modules you expect to create or change;
- a validation and acceptance-testing strategy;
- any requirement that needs an equivalent implementation because the literal behavior is not reliable in Excel.

Do not reduce the confirmed scope. If you recommend phasing, distinguish implementation sequence from final scope. Wait for explicit approval before execution.

All technical documentation, code comments, configuration keys, logs, and tests must be in English. The user-facing workbook interface remains in Simplified Chinese.
